@extends("layout")
    @section("content")
        @if(isset($user))
            <p>
                {{$user->unique_id}}
                {{$user->username}}
            </p>
        @else
            @dd("kurac")
        @endif
    @endsection
